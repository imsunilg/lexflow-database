# 11_Views

Regular (non-materialized) views, e.g. v_calendar_items unifying calendar events, hearings, important dates and due tasks. Populated in a later Build Playbook prompt (DB-12).

This folder is currently empty — SQL has not been written yet.
