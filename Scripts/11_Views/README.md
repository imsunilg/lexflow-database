# 11_Views

Plain (non-materialized) SQL views composing data across schemas, e.g. v_calendar_items
(a UNION of calendar_events, hearings, matter_important_dates, tasks-with-due-date).
Populated in Phase B (PROMPT DB-12). Empty for now.
