# 14_Triggers_Global

Shared/global trigger functions (e.g. trg_set_updated_at) plus the scripts that
attach them across every mutable table in the database. Object-specific triggers
live instead in each object's own 004_Triggers.sql. Populated in Phase B
(PROMPT DB-13). Empty for now.
