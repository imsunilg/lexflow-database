# 14_Triggers_Global

Cross-cutting trigger functions applied to many tables (e.g. trg_set_updated_at) plus the script attaching them across schemas. Populated in a later Build Playbook prompt (DB-13).

This folder is currently empty — SQL has not been written yet.
