# 00_Extensions

PostgreSQL extensions required by the schema (pgcrypto, uuid-ossp, pg_trgm, ltree, btree_gist). Runs first, before any schema or table exists.

This folder is currently empty — SQL has not been written yet.
