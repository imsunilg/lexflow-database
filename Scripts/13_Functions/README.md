# 13_Functions

Reusable SQL functions: number-series generator (fn_next_number), time-entry rate resolution (fn_resolve_time_entry_rate), etc. Populated in a later Build Playbook prompt (DB-13).

This folder is currently empty — SQL has not been written yet.
