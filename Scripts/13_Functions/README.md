# 13_Functions

Reusable PL/pgSQL functions shared across schemas, e.g. fn_next_number
(number-series generator) and fn_resolve_time_entry_rate (billing rate
precedence chain). Populated in Phase B (PROMPT DB-13). Empty for now.
