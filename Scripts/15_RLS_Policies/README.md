# 15_RLS_Policies

Row-Level Security enablement and tenant_isolation policies for every tenant-scoped table, one script per schema. Populated in a later Build Playbook prompt (DB-11).

This folder is currently empty — SQL has not been written yet.
