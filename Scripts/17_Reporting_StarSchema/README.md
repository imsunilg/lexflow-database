# 17_Reporting_StarSchema

Denormalized fact/dimension tables for reporting (rpt_dim_date, rpt_dim_lawyer,
rpt_dim_client, rpt_dim_practice_area, rpt_fact_billing, rpt_fact_time,
rpt_fact_matters), populated by an hourly incremental job in LexFlow.Workers
(not built here). Populated in Phase B (PROMPT DB-15). Empty for now.
